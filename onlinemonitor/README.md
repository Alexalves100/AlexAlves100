# OnlineMonitor - Screenshots Placeholder

Este diretório deve conter as imagens (screenshots) do projeto OnlineMonitor para o README principal.

## Imagens Necessárias

1. `gui_main.png` - GUI principal do OnlineMonitor (painel de status)
2. `gui_logs.png` - Aba de logs em tempo real
3. `service_install.png` - Instalação como Windows Service (CMD Admin)

## Como Obter

1. Execute o projeto localmente (`go run main.go` ou compile `go build -ldflags="-H=windowsgui"`)
2. Navegue pela interface gráfica
3. Capture screenshots (Win+Shift+S no Windows)
4. Salve neste diretório com os nomes acima
5. As imagens serão referenciadas automaticamente no README.md principal

## Dimensões Recomendadas
- Largura: 800-1200px
- Formato: PNG (preferencial) ou JPG
- Proporção: 16:9 ou 4:3